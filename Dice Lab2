
public class Dice 
{
	private int average;
	
	public int Throw(int bounces)
	{
		for (int i = 0; i < bounces; i++ )
		{
			average = average + (1 + (int)( Math.random()* 6));
		}
	return average/bounces;
	}
}
